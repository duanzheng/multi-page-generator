import React, { PureComponent } from 'react';
import ReactDOM from 'react-dom';

class HardwareDigitalWallet extends PureComponent {
  render() {
    return <div>HardwareDigitalWallet</div>;
  }
}

ReactDOM.render(<HardwareDigitalWallet />, document.getElementById('root'));
